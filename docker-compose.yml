services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: fieldbook
      POSTGRES_PASSWORD: fieldbook
      POSTGRES_DB: fieldbook
    ports:
      - "5432:5432"
    volumes:
      - fieldbook_pg:/var/lib/postgresql/data

volumes:
  fieldbook_pg:
